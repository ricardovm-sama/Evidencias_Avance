import { FiltrotempingPipe } from './filtrotemping.pipe';

describe('FiltrotempPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrotempingPipe();
    expect(pipe).toBeTruthy();
  });
});
