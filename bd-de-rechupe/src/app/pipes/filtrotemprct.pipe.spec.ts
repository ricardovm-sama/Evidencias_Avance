import { FiltrotemprctPipe } from './filtrotemprct.pipe';

describe('FiltrotemprctPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrotemprctPipe();
    expect(pipe).toBeTruthy();
  });
});
